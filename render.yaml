# Render blueprint: managed Postgres + backend API.
# Deploy: https://dashboard.render.com → New → Blueprint → connect this repo.
# After first deploy, set on the web service:
#   ENTSOE_API_KEY, JWT_SECRET, ALLOWED_ORIGINS (frontend URL),
#   RESEND_API_KEY, FRONTEND_BASE_URL, Email__FromAddress=onboarding@resend.dev
# Keep EMAIL_BYPASS unset or false when using Resend.

databases:
  - name: energy-monitor-db
    plan: free
    databaseName: energy_monitor
    user: energy

services:
  - type: web
    name: energy-monitor-api
    runtime: docker
    plan: free
    rootDir: backend
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: ASPNETCORE_URLS
        value: http://+:8080
      - key: JWT_SECRET
        generateValue: true
      - key: ENTSOE_API_KEY
        sync: false
      - key: ALLOWED_ORIGINS
        sync: false
      - key: RESEND_API_KEY
        sync: false
      - key: FRONTEND_BASE_URL
        sync: false
      - key: Email__FromAddress
        value: onboarding@resend.dev
      - key: EMAIL_BYPASS
        value: "false"
      - key: ConnectionStrings__DefaultConnection
        fromDatabase:
          name: energy-monitor-db
          property: connectionString
